Telegram SOCKS5 Proxy в Docker

Готовый к использованию SOCKS5 прокси-сервер на основе Dante для разблокировки Telegram.
При каждом запуске контейнера автоматически генерируется случайный пароль и готовая ссылка для Telegram.

🚀 Быстрый старт (одна строка)
Скопируйте и выполните эту команду:


git clone https://github.com/cmzmozg/telegram-socks5-proxy.git && cd telegram-socks5-proxy && chmod +x entrypoint.sh && docker-compose up -d && sleep 5 && docker-compose logs --tail=20

Что происходит:

Клонируется репозиторий
Переход в папку проекта
Права на выполнение для entrypoint.sh (важный шаг!)
Запуск контейнера в фоне
Ожидание 5 секунд и вывод лога со ссылкой
Через несколько секунд вы увидите в выводе ссылку вида:

tg://socks?server=123.456.789.0&port=1080&user=proxy&pass=СлучайныйПароль

Скопируйте её, вставьте в любой чат Telegram (например, "Избранное") и нажмите — прокси активируется.

🔧 Как это работает:

При сборке образа устанавливается Dante и утилиты.
При каждом запуске контейнера:
Определяется внешний IP сервера
Генерируется случайный 16-символьный пароль
Создаётся пользователь proxy с этим паролем
В лог выводится готовая ссылка для Telegram
Прокси запускается на порту 1080.

Требования:

Установленные Docker и Docker Compose
Публичный IP-адрес (или проброс порта 1080)
🔐 Безопасность
Пароль генерируется случайным образом при каждом запуске и выводится только один раз — в лог.
Никто не сможет подключиться к вашему прокси без этого пароля.