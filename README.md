# Telegram SOCKS5 Proxy (Dante) in Docker

🚀 Готовый к использованию SOCKS5-прокси на базе **Dante** для разблокировки Telegram с автоматической генерацией пароля и ссылки подключения.

---

## ✨ Особенности

- Автоматическая генерация **уникального 16-символьного пароля** при каждом запуске контейнера
- Мгновенный вывод готовой ссылки `tg://socks` в логах
- Минималистичный образ на базе Alpine Linux с Dante SOCKS5-сервером
- Простое развёртывание через Docker Compose — без сложных настроек
- Без сохранения пароля: безопасность через однократную генерацию

---

## 🚀 Быстрый старт (одна команда)

```bash
git clone https://github.com/cmzmozg/telegram-socks5-proxy.git && \
cd telegram-socks5-proxy && \
chmod +x entrypoint.sh && \
docker-compose up -d && \
sleep 5 && \
docker-compose logs --tail=20


Через несколько секунд в выводе появится ссылка вида:

tg://socks?server=123.456.789.0&port=1080&user=proxy&pass=AbCdEfGhIjKlMnOp

Скопируйте её, вставьте в любой чат Telegram (например, «Избранное») и нажмите — прокси активируется мгновенно.

Как это работает
При старте контейнера скрипт entrypoint.sh определяет публичный IP-адрес хоста.
Генерируется криптостойкий пароль (16 символов, алфавитно-цифровой).
Пользователь proxy добавляется в систему контейнера с этим паролем.
Dante запускается в режиме sockd с аутентификацией по логину/паролю.
Готовая ссылка для Telegram выводится в stdout — её можно получить командой:

docker-compose logs | grep "tg://socks"

Требования
Установленные Docker и Docker Compose
Сервер с публичным IPv4-адресом или проброшенный порт 1080 на роутере/фаерволе
Открытый порт 1080/TCP в облачном фаерволе (если используется облако: AWS, GCP, Yandex Cloud и др.)

🔐 Безопасность
Пароль генерируется случайно при каждом запуске контейнера через /dev/urandom.
Пароль не сохраняется в файловой системе — только выводится в лог при старте.
Аутентификация обязательна: без логина proxy и пароля подключение невозможно